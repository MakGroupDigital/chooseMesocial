rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DÉVELOPPEMENT UNIQUEMENT - Règles permissives
    // Permet tout pour le développement
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
